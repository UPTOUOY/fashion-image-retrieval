# 이미지 임베딩 유사도 검색 — 실험 기록

> DeepFashion In-Shop · query 500 상품 / gallery 8,000장 (train·eval 상품 분리, leakage 없음)
> 지표: Recall@1/5/10, mAP@10 · 임베딩 L2정규화 + FAISS 코사인

---

## 1. 개선 사다리 (핵심 결과)

| 단계 | 학습 | R@1 | R@5 | R@10 | mAP@10 | 판정 |
|---|---|---|---|---|---|---|
| DINOv2 zero-shot | 없음 | 0.492 | 0.664 | 0.726 | 0.200 | baseline |
| + center-crop | 전처리 | 0.494 | 0.662 | 0.726 | 0.200 | ❌ 기각 |
| + head-only fine-tune | 얼린 백본 + head | 0.696 | 0.886 | 0.930 | 0.408 | ✅ |
| + backbone fine-tune (DINOv2-S) | 백본 직접 | 0.852 | 0.946 | 0.962 | 0.553 | ✅ |
| + backbone fine-tune (DINOv2-B) | 더 큰 백본 | 0.874 | 0.954 | 0.968 | 0.600 | ✅ |
| **+ 강화 (24k장·6ep·6블록)** | | **0.884** | **0.962** | **0.980** | **0.621** | ✅ 최종 |

**최종: Recall@1 0.492 → 0.884 (+80%), mAP@10 0.20 → 0.621 (3.1×), top-5 실패 168 → 19.**

---

## 2. 실패 분석 (원인 규명)

- top-5에 정답 없는 실패 **168 / 500**. 자동 태그(샘플 60): cross_category 37 / same_category 23.
- **진짜 원인**: 모델이 "옷"이 아니라 **"장면 전체(사람·포즈·구도)"**를 매칭.
  - 결정적 증거: "의자에 앉은 사람" query → top-1~5가 **전부 의자에 앉은 사람** (옷은 제각각).
  - "어두운 상의+밝은 하의" 전역 색배치, "옆모습 걷는 포즈"로 뭉침. 전신샷이라 옷이 화면 일부.
- 처방: (1) center-crop → **효과 없음(기각)**, (2) 같은 상품 다른 포즈를 가깝게 **학습 → 성공**.

---

## 3. Re-ranking 실험 (기각)

강화런 임베딩(0.884) 위에 검색 후처리 적용:

| | R@1 | R@5 | R@10 | mAP@10 |
|---|---|---|---|---|
| best (재순위 없음) | 0.884 | 0.962 | 0.980 | 0.621 |
| + αQE (topk=10, α=3) | 0.724 | 0.894 | 0.944 | 0.514 |
| + k-reciprocal (k1=20,k2=6,λ=0.3) | 0.798 | 0.926 | 0.952 | 0.585 |
| + αQE (topk=3, α=1) | _채워넣기_ | | | |

- **판정: 기각.** 둘 다 성능 하락.
- **이유**: re-ranking은 person-ReID처럼 **query당 정답이 많은** 상황에 강함. 우리는 query당 정답이 몇 장뿐 + 이미 임베딩이 강해서, 이웃 보강이 distractor를 끌어와 노이즈만 추가.
- **교훈**: 최신 기법도 데이터 구조에 안 맞으면 기각. 상황에 맞는 기법 선택.

---

## 4. 도메인 특화 모델 — FashionCLIP ✅

가설: 패션 데이터로 학습된 임베딩이 범용 DINOv2보다 상품 검색에 유리한가?

**같은 split·같은 세션 비교 (완전 공정, apples-to-apples):**

| 모델 | 학습 | R@1 | R@5 | R@10 | mAP@10 |
|---|---|---|---|---|---|
| DINOv2 zero-shot | 없음 | 0.538 | 0.710 | 0.790 | 0.212 |
| DINOv2-small fine-tune | 백본 supcon | 0.882 | 0.954 | 0.968 | 0.577 |
| **FashionCLIP zero-shot** | **없음** | **0.892** | 0.962 | 0.964 | 0.562 |
| **FashionCLIP fine-tune** | supcon | **0.932** | 0.976 | 0.982 | 0.663 |

- **킬러 인사이트: FashionCLIP은 학습을 전혀 안 하고도(zero-shot 0.892) 백본 fine-tune한 DINOv2(0.882)를 이미 넘어섰다.**
  → "며칠 걸려 튜닝한 범용 모델 ≈ 아무것도 안 한 도메인 모델". **표현(representation) 선택이 아키텍처 튜닝만큼의 가치.**
- 학습 0으로 모델만 바꿔 R@1 0.538 → 0.892 (**+66%**). 도메인 표현의 힘.
- FashionCLIP도 fine-tune하면 0.892 → 0.932 추가 개선(같은 supervised-contrastive 방식).
- **시사점: 상품 검색은 아키텍처 튜닝보다 "도메인 표현 선택"이 우선.** 다양한 CV 기술을 문제에 맞게 골라 적용하는 판단. (쇼포트 JD 직결)

> 참고: 이전 세션(다른 split, baseline 0.492)의 DINOv2-B 강화런 0.884는 split이 달라 직접 비교 대상에서 제외. 위 표는 전부 **이번 세션 같은 split**이라 그대로 맞대면 가능.

---

## 5. Attention 시각화 ✅

- fine-tune 전/후 DINOv2-small의 patch–CLS 집중도 히트맵 비교 (query 4장).
- **관찰**: fine-tune **전**엔 반응이 **배경 전반 + 인물 윤곽 halo**에 퍼져 있음(배경이 뜨거움) → §2의 "장면 전체 매칭" 편향과 일치. **후**엔 **배경이 식고(파랑) 반응이 옷·인물로 모임.** 특히 반다나 티는 옷 프린트·몸통에 집중이 뚜렷, 나머지도 배경 halo가 감소.
- **해석**: "100% 옷만" 보는 건 아니지만 **배경 억제 + 옷/인물로 집중 이동**이 분명 → §2 "장면 매칭 → 옷 매칭" 개선을 시각적으로 뒷받침. (같은 split DINOv2-small fine-tune = §4 표의 0.882 모델)
- 결과 이미지: `results/attention_compare.png` (포폴: `img_retrieval/attention_compare.png`)

---

## 6. 핵심 역량 요약

- 이미지 임베딩 · 유사도 검색(FAISS) · Recall@k/mAP 평가
- **실패 분석 → 원인 규명 → 겨냥한 학습**으로 +80% 개선
- Metric learning (supervised contrastive) · 백본 fine-tune · 도메인 특화 모델 비교
- 기각(center-crop, re-ranking)까지 수치로 기록 — 상황에 맞는 기법 선택 판단력
